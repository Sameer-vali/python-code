a=int(input("Enter a first number:"))
b=int(input("Enter a second number:"))
big=max(a,b)
step=big
while True:
    if big%a==0 and big%b==0:
        break
    else:
        big=big+step
print("LCM=",big)
